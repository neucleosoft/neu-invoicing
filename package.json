{
  "name": "neu-invoicing-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@11.1.3",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "test": "pnpm --dir packages/shared test && pnpm --dir apps/desktop verify:parity",
    "dev": "turbo run dev",
    "build": "turbo run build",
    "build:win": "turbo run build:win",
    "prisma:generate": "turbo run prisma:generate",
    "prisma:migrate": "turbo run prisma:migrate",
    "prisma:studio": "turbo run prisma:studio"
  },
  "devDependencies": {
    "turbo": "^2.9.14"
  }
}
