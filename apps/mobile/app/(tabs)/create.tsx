// Placeholder route for the center "Create" tab. The tab's button (defined in
// _layout) intercepts the press and routes to New Invoice, so this is never
// rendered — it exists only so the Tabs.Screen has a backing route.
export default function CreatePlaceholder() {
  return null;
}
